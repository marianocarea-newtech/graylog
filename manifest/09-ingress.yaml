apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: graylog-ingress  
spec:
  rules:
  - http:
      paths:
      - path: /v1
        pathType: Prefix
        backend:
          service:
            name: graylog
            port:
              number: 9000
